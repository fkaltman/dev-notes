# dev-notes

> This repository is a public development scratchpad used to document learning,
> refactors, and ongoing technical exploration.

## Creating a New Branch with a Worktree

To create a new branch and separate worktree:

```bash
git worktree add -b simplify-refactor ../simplify-worktree main
